version: '3.9'
services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app
    environment:
      - LLM_PROVIDER=fallback

  frontend:
    build: ./frontend
    ports:
      - "3000:80"
    depends_on:
      - backend
